import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Log in to Framesbook",
  description: "Let's help you get back to Framesbook",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return
  ;<>{children}</>
}
